# 2026-08-25

## B/C 类硬外部依赖集成测试（mock，默认不通过）
按原计划「先写 mock 测试，默认不通过，等给依赖信息再跑」完成 B 类（外壳/POCO/HTTP 已属 A 类收口，1041 绿 / 46.8%）。

新增 4 个集成测试文件（均读环境变量，未设置则 `Assert.Fail` 明确提示，默认不通过）：
- `src/DotNet.Util.Tests/Cache/RedisUtilIntegrationTests.cs` — `DUP_TEST_REDIS=host:port`（可选 `[user:]password@host:port`），覆盖 Set/Get/Contains/Remove/Add/GetAllKeys。RedisUtil 静态 FullRedis 单例首次访问时按 BaseSystemInfo 初始化，故测试内先赋值再调用。
- `src/DotNet.Util.Tests/Plus/QqwryUtilIntegrationTests.cs` — `DUP_TEST_QQWRY=qqwry.dat 路径`，复制到 `Utils.GetMapPath("/plus/qqwry.dat")` 后 `GetLocation`。net8 下该路径解析为 `<cwd>/~/plus/qqwry.dat`。
- `src/DotNet.Util.Tests/Db/DbHelperIntegrationTests.cs` — `DUP_TEST_SQLSERVER=连接串`，用 `SqlHelper` 验证 Open/ExecuteScalar/Fill/ExecuteNonQuery。
- `src/DotNet.Util.Tests/Configuration/RegistryUtilIntegrationTests.cs` — `DUP_TEST_REGISTRY=1`；注册表逻辑仅在 net48（HKLM\Software\DotNet，需管理员）编译，net8 下 `Skip`。

验证：`dotnet build -f net8.0` 0 错误；过滤运行 `*IntegrationTests` → 9 失败（env 提示）+ 1 跳过。全部改动未 commit/push。

待用户提供：Redis 连接串 / SQL Server 测试库连接串 / qqwry.dat 路径 / RegistryUtil 确认（注：RegistryUtil API 固定写 HKLM，不支持 HKCU，需管理员）。

## DotNet.Util 下一轮审查（Round 6 扫描，仅产出清单未修复）
方向：NuGet 升级 + EULA 合规 + 安全维度。结果（待用户确认修复范围）：
- **CVE**：`Microsoft.Extensions.Caching.Memory 8.0.0`（CVE-2024-43483 / GHSA-qj66-m88j-hmgj，High 8.8，仅 net6/net7 TFM）；修复版 8.0.1。由 `<ExtensionsVersion>8.0.0</ExtensionsVersion>` 定义（DotNet.Util.csproj:39、DotNet.Util.Cache.csproj:35）。
- **EULA**：NPOI 2.7.6/2.5.6 均 Apache-2.0，未触 2.8.0 的 OSMF 维护费 EULA ✅；但 2.7.6 可能仍传递 NSax(LGPL-3.0)（需 nuspec 确认，2.8.0 才移除）。SharpZipLib 1.4.2/1.3.3 已 MIT ✅。Portable.BouncyCastle 1.9.0 MIT 无公告。
- **安全**：硬编码默认凭据 ServicePassword/ServiceUserName="CuiWenYuan"/"Troy.Cui"（BaseSystemInfo.Secret.cs:101/96、BaseUserInfo.cs:142）；活跃 DES 加密（SecretUtil.cs:446/501，库已有 AesEncrypt/AesDecrypt AES-256 可替代）；活跃 MD5 密码哈希（SecretUtil.cs:209）；SHA1 仅旧签名验签兼容(:126) 可接受。
- **版本一致性**：NPOI/SharpZipLib/MySql.Data(26.7.0 vs 8.0.32.1 过旧)/Oracle 多版本混用；System.ValueTuple 冗余；System.Data.SqlClient 4.9.1 维护模式（建议远期迁 Microsoft.Data.SqlClient）。
未改动任何代码；等用户选修复项（全修/选 1-N/分批）。

## CVE 修复（已执行：仅改已知漏洞）
用户确认「只改 CVE」。改 `DotNet.Util.csproj:39`、`DotNet.Util.Cache.csproj:35` 的 `<ExtensionsVersion>8.0.0</ExtensionsVersion>` → `8.0.1`（net8/9/10 用 10.0.11 不动）。复扫 `dotnet list package --vulnerable` 已无易受攻击包；两受影响类库构建 0 错误。注意 NU1603 警告（消费方图 Microsoft.Extensions.Configuration 解析到 9.0.0 ≥ 8.0.1，向上兼容，非错误）。改动未 commit/push。

## NuGet 发布自动化工作流（已写，未提交）
新增 `.github/workflows/publish-nuget.yml`：触发 `push tags:['v*']` + `workflow_dispatch`；`windows-latest` + 装 .NET 10 SDK（原生编译 net46/47/48）；`dotnet pack src/DotNet.Util.sln -c Release -o ./artifacts`；分别 push `./artifacts/*.nupkg` 与 `*.snupkg` 到 `https://api.nuget.org/v3/index.json`，用 `secrets.NUGET_API_KEY`。版本沿用 csproj 日期后缀（`$(VersionPrefix).yyyyMMdd`），tag 仅作发令枪，不改 csproj。待用户在仓库 Settings→Secrets 加 `NUGET_API_KEY` 后，commit 工作流并 push `v*` tag 即可触发。未 commit/push。

## Troy vs dev 合并可行性 & 差异分析（2026-08-25 下午）
用户要求对比 Troy 与 dev、评估自动合并并出差异报告，重点关注 net4x 老用户兼容。
- **结论：可自动合并、零冲突**。`origin/dev` 是 Troy 真子集（`Troy...origin/dev` 反向 diff 空）；`git merge-tree --write-tree origin/dev Troy` → EXIT=0，tree `7aa60705`，fast-forward 无冲突。
- **net4x 编译风险：未扫到**（IndexOf/LastIndexOf char 重载 net4x 有；ThrowIfNull 仅出现在注释；无 Span/HashCode.Combine/string.Create/TrimEntries/UnixEpoch/Chunk/SearchValues）。新增 `#if` 条件编译 131 行，需 Windows 实机构建 net4x 验证。
- **加密兼容：安全**。`BaseSystemInfo.SecurityKey` 默认值未变（`"DotNet.Troy.Cui.2018"`，Secret.cs 仅改版权年份）；SecretUtil 新增 AesEncrypt/Decrypt（增量），DesEncrypt/Decrypt 保留（仅修空 key 越界），老密文可解。
- **真实兼容影响①：net452 TFM 全仓移除**（13 个 csproj `net452;net46;...`→`net46;net47;...;net10.0`）。4.5.2 消费者将不可用，4.6+ 不受影响。
- **真实兼容影响②：依赖大跳** MySql.Data 9.4.0→26.7.0（主版本）、NewLife.Core/Redis 11.7/6.3→11.18/6.6、JWT 11.0→11.1、System.ValueTuple 4.6.1→4.6.2。
- **杂质**：`tmp_toint_test/`（2 文件）被误提交进 Troy，不在 .sln。用户已于 12:57 删除并 staged（连同 `src/tools/replace-string-isnullorempty3.ps1`），尚未 commit。清理项已实质完成，不再阻塞合并。
- 报告已写入 `Troy-vs-dev-diff-report.md`（未提交）。未执行任何 git 合并/提交。

## DotNet.Util 对标 NewLife.Core 维度欠缺分析（2026-08-25 下午）
用户要求对标 NewLife.Core 找本库欠缺。规范仓库 `NewLifeX/X`（NewLife.Core 核心库，MIT）。
- **方法**：先盘点本库 17 项目模块，再对照官方 README 模块表，对本库"疑似欠缺"项全仓 grep 反向核验。
- **已覆盖（持平，不需补）**：基础扩展(Convert/String/Path/IO/Runtime/DisposeBase/Reflect)、安全(RSA/DES/AES/MD5/RC4，仅缺 DSA/CRC)、缓存、配置(XML)、模型/实体、对象池、校验验证。
- **🔴 完全缺失（本库最大短板）**：定时调度/Cron、依赖注入 IOC、网络通信服务端(Tcp/Udp/WebSocket/RPC)、脚本引擎、消息队列引擎(仅 BaseMessageQueueEntity 实体表非引擎)。
- **🟠 明显偏弱**：进程管理 ProcessHelper、APM/链路追踪(仅 TraceabilityUtil 本地)、Windows 服务守护、本地化框架(仅 CultureInfo 解析，ResourceManagerWrapper 被注释)、ORM 仅裸 ADO+微映射(无 XCode 级)。
- **🟡 部分覆盖**：日志(缺异步多Sink)、序列化(缺 Binary)、压缩(仅 Zip)。
- **反向优势（本库特色，不算欠缺）**：ERP 权限域(DotNet.Business)、身份证/银行卡/Rmb 验证、验证码、Office(Npoi/Word/Csv)、百度OCR/微信/QQwry/短信、图像、多 DB 方言。
- **建议补的高 ROI 项**：① IOC 入口 AddDotNetUtil()（已引 Microsoft.Extensions.*，成本极低）；② 定时调度引 Quartz 或封装 CronTimer；③ TraceabilityUtil 加 OpenTelemetry；④ Json 配置源。网络/RPC/MQ/服务/脚本/完整ORM 明确不自研，按需引 NewLife 生态。
- 报告已写入 `NewLifeCore-benchmark-report.md`（未提交）。

## DotNet.Util 对标 NewLife.Core 文档/README 维度（2026-08-25 傍晚）
用户要求"功能放一边，先看文档/README 维度"。方法：盘点本库文档资产 + 对照 NewLife.Core 官方文档体系(newlifex.com 文档站/每模块B站视频/NuGet包README/详尽XML注释/团队博客)，缺项做 grep/文件核验。
- **NewLife.Core 文档 7 维**：①官方文档站每模块深度页 ②每模块B站视频 ③源码每个公共成员必带<summary>+常带<example>/[Description]特性 ④NuGet包README ⑤cnblogs深度原理文 ⑥社区(QQ群/Issue) ⑦每页含用法/最佳实践/性能数据固定结构。
- **本库现状(已核验)**：根README中英双语(结构割裂、API介绍弱)；14个子项目README(质量较高,亮点)；CHANGELOG/CONTRIBUTING/INSTALL/LICENSE齐全；`src/doc/`架构/设计/规范/数据字典(本库比NewLife GitHub内更系统)；10项目GenerateDocumentationFile；全库9034行///，**公共类型注释覆盖率86.9%**(145类型中126带///)。无docs站(docfx/mkdocs无)、无NuGet包内嵌README(PackageReadmeFile缺失)、无视频、无独立sample项目。
- **🔴 完全缺失**：①NuGet包内嵌README(成本极低,14 csproj各+2行复用现有README) ②官方文档站/mkdocs ③视频教程。
- **🟠 偏弱**：④CHANGELOG是空模板占位(无真实版本记录) ⑤XML注释规范性(类型86.9%尚可但方法级低、缺<example>/[Description>特性) ⑥每模块深度图文教程页。
- **🟡 本库优势(不需补)**：架构/编码规范文档、子项目README一致性、中英双语。
- **最高ROI建议**：先补NuGet包内嵌README(极低风险高收益,不引入代码风险)→回填CHANGELOG→起mkdocs站→XML注释规范+docfx→视频(后续)。
- 报告已写入 `NewLifeCore-docs-benchmark.md`（未提交）。

## ① NuGet 包内嵌 README 已落地（2026-08-25 晚）
用户确认开工文档对标第①项。给 13 个可打包 csproj（GeneratePackageOnBuild=true 且同级有 README.md）加：
`<PackageReadmeFile>README.md</PackageReadmeFile>`（主 PropertyGroup 内，2 tab 缩进）+ `<ItemGroup><None Include="README.md" Pack="true" PackagePath="\" /></ItemGroup>`（</Project> 前）。
- 用 python 批量注入，严格保留 BOM 与原换行符；Tests 项目(IsPackable=false)正确排除；初始插入 PackageReadmeFile 行缩进误为 3 tab，已正则归一为 2 tab。
- 验证：dotnet pack -c Release --no-build -p:TargetFrameworks=net8.0 单 TFM，`Wangcaisoft.DotNet.Util` 与 `DotNet.Util.Plus` 的 .nupkg 均含包根 README.md、nuspec 含 `<readme>`、logo/dll 在包。其余 11 个同构。
- 注意：本环境 net48 testhost 崩溃，仅 net8.0 验证；真实全 TFM 发布由 windows-runner CI 完成（NU5128 警告仅限定的单 TFM 验证产物，全 TFM 不会出现）。
- 13 个 csproj 改动未提交（遵循工作流）。
