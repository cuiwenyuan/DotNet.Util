# 2026-08-30

## P2（注释/健壮性）4 项全部搞定（用户"把 P2 注释/健壮性 搞定"）
- #8 `IsIpv4(null)` 抛异常 → 随 Bug 4（IsIpv4 改 `IPAddress.TryParse`）一并修复（前次）。
- #9 `GetStartTime` XML summary 误写"获取结束时间" → 随进展 2（GetStartTimeOfDay 新增）一并修复（前次）。
- #10 `ValidateUtil.cs` IsIdCard 15位注释"默认补足为19世纪" → 改为"默认补足为 19xx 年（即 20 世纪）"（纯注释，inspection 确认）。
- #11 `DateUtil.cs:172` `GetDaysOfMonth(int,int)` 非法月份（0/13/负数）**静默返回 0** → 在 switch 前加 `if (month < 1 || month > 12) throw new ArgumentOutOfRangeException(...)`（快速失败）。`DateTime` 重载的 `month` 取自 `dt.Month` 恒为 1-12，无需改。

**改动文件**：`src/DotNet.Util/Util/ValidateUtil.cs`(#10)、`src/DotNet.Util/Util/DateUtil.cs`(#11)、`src/DotNet.Util.Tests/Util/DateUtilTests.cs`(加 `using System;` + 新增 `GetDaysOfMonth_InvalidMonth_Throws` 单测，覆盖 0/13/-1 抛异常)。

**验证受阻与替代方案**：本机 IDE（WorkBuddy 桌面）锁住 `src/DotNet.Util/obj/{Debug,Release}/net8.0/DotNet.Util.AssemblyInfoInputs.cache`（deny-write），导致仓内 `dotnet test -c Debug/-c Release -f net8.0` 均 MSB3491 "Access is denied" 无法构建。`dotnet build-server shutdown` 已成功但仍锁 → 确认为 IDE 持锁（非构建服务器）。改用 **C:\Temp\verify_p2 独立控制台**（复制 `GetDaysOfMonth` 方法体 + `IsRuYear`）实测全绿：合法月份值正确、0/13/-1 均抛 `ArgumentOutOfRangeException`。仓内 xunit 单测待锁释放后 `dotnet test -f net8.0` 可跑通。

**踩坑**：用 `-p:BaseIntermediateOutputPath=verify_obj/ -p:BaseOutputPath=verify_bin/` 重定向规避锁时，触发 CS0579 重复特性（旧 `obj\Release\net8.0\AssemblyInfo.cs` 仍被编译 + 新 verify_obj 生成同名）→ 不可行；最终走独立 harness 验证。

**遗留垃圾**：上述重定向在仓内留下 10 个 stray 目录（`src/DotNet.Util[.Cache/.Db/.Plus/.Tests]/{verify_obj,verify_bin}`），同样被 IDE 锁住无法 `rm`（genie-trash 拦截且 denied）。属未跟踪构建产物、不影响真实构建（用 `obj\`），待 IDE 释放后清理（关解决方案/重启即可删除）。

**Round7 报告** `CodeReview-Round7-hidden-bugs.md` 已标记 P2 #9/#10/#11 为已修复、footer 更新修复进度。所有改动均未 commit/push（遵守用户"不自动提交"规则）。
