# 2026-09-19 工作日志

## SystemCode #5 范围 B + #7 参数化（已落地未提交）

- 文件：`src/DotNet.Business/BaseUserRole/BaseUserRoleManager.cs` 的 `GetDataTable(bool, string systemCode=null)`。
- 改动：新增可选 `systemCode` 形参；读过滤改为 `(SystemCode=@SystemCode OR SystemCode='Base') AND Enabled=1 AND Deleted=0`；缓存键并入 `effectiveSystemCode`（= systemCode ?? BaseSystemInfo.SystemCode ?? "Base"）。
- #7 参数化：`dbHelper.GetParameter(FieldSystemCode)` 占位 + `dbHelper.MakeParameters(...)` 绑定，不再拼接。
  - ⚠️ 关键坑：KVP 版 `GetDataTable` 会经 `GetWhereString` 自动追加 `SystemCode=@SystemCode`，若同时手写 OR 条件会生成 `(A OR B) AND A` 误抵消 `OR 'Base'` → 故直接 `DbHelper.Fill("SELECT * FROM "+CurrentTableName+" WHERE "+sb, dbParameters)`。
- 范围 B 未改：`GetDataTableByPage`（`override` 虚方法签名锁死，Mode B 分页仍泄漏）、`RemoveCache`（仍全局 `BaseSystemInfo.SystemCode`）、#6 默认构造（用户明确不做）。
- 验证：net8.0 / net48 均 0 错误（net48 补 `<param name="systemCode">` 消 CS1573）。
- 文档：`SystemCode-Coverage-Analysis.md` §6.5 待办 + §6.6 修订记录。
- 未自动 git commit/push（遵循约定，待用户确认）。
- 二次复核（用户要求“重新检查再更新”）：核对 §6.1 行号（:161/:217/:241/:1179/:1231 均命中）；确认 `BaseUserRoleManager.GetDataTable` 是全仓唯一带 `systemCode` 参数的 `GetDataTable` 且本仓无调用方；将 §6.5 改为滚动待办（#5/#7 标记部分完成、剩 `GetDataTableByPage`+`RemoveCache`+其它拼接点），§6.4 警告数 539→540 对齐。文档已自洽。

## 用户精简删除 BasePermissionScope 子系统（指令⑤：重新审查代码 + 更新文档）

- 用户提交 `0cb4906`「删除BasePermissionScopeEntity相关的代码…精简再精简」整删 `BasePermissionScope` 子系统（`BasePermissionScopeManager.*.cs` −1484/−145/−119、`BasePermissionScopeEntity.Auto.cs` −240），并回退 `BaseUserRoleManager.GetDataTable`（:105 回到原始形态，无 systemCode 形参、缓存键无子系统）。
- 后续提交 `8818ee3`「删除GetPermissionScopeTableName」删除 `BaseManager.TableName.cs` 中的 `GetPermissionScopeTableName`（及更早已不存在的 `GetOrganizationScopeTableName`）。现 `GetXTableName` 仅存 5 个：Module/Permission/Role/RoleOrganization/UserRole。
- 源码 grep 复核：`*.cs` 中已无 `BasePermissionScope` 引用、`GetPermissionScopeTableName` 仅存于 `bin/obj` 旧 XML 产物。
- `SystemCode` 双模式基础设施仍存活于 BaseModule/BasePermission/BaseRole/BaseUserRole/BaseUser/BaseLogonLog/BaseParameter/BaseOperationLog/BaseUserLogon/BaseChangeLog（读/写路径）；#7 拼接隐患仍在 BaseModule/BaseLogonLog/BaseParameter/BaseRole/BaseUser。
- 文档 `SystemCode-Coverage-Analysis.md` 已重写对齐：§0 表名函数改 5 个并注删除项；§1 #9~13 标“已删除”；§2.3 注硬编码 FROM 已随子系统消失；§3 #1~#4 标“已随子系统移除”、#5 标“回退为原始形态 🟠”、#6/#7 标“未处理”；§5/§6.1~§6.6 加“历史/代码载体已删除”横幅并标注 GetDataTable 改造已回退；新增 §7「当前状态」含提交清单/存活范围/孤儿方法/#1~#7 剩余状态/验证（net8.0/net48 均 0 错误，29 警告 NU1603 无关）。
- 验证：net8.0 / net48 均 0 错误（29 警告，NU1603 版本解析，与本次无关）。
- 未自动 git commit/push（遵循约定，待用户确认）。

## 用户目标澄清：最终要 Mode B（整合 Base Table + SystemCode 列区分）

- 用户明确：当前跑 Mode A（分表，SystemCode="Business"→物理 BusinessXxx 表，工作正常）；最终目标 `UseBaseTable=true`，所有子系统共用 BaseXxx 统一表，仅靠 SystemCode 列区分。问“切到 Mode B 后当前代码有无潜在 Bug”。
- 机制：Mode B 下 `GetXTableName` 对所有 systemCode 返回 BaseXxx（表名不再隔离），隔离完全依赖 SystemCode 列。两大铁律：读必须 `WHERE SystemCode='x'`、写必须 `entity.SystemCode='x'`。
- 关键代码核查：
  - 实体 `SystemCode` 默认均为 `"Base"`（BaseModule/BaseRole/BaseUserRole/BasePermission/BaseParameter/BaseOperationLog/BaseChangeLog/BaseOrganizationScope 等，grep Auto.cs 确认）。
  - `BaseManager` 基类**无**集中 stamp SystemCode 机制（grep `BaseManager*.cs` 仅 `SaveEntityChangeLog` 用全局 `BaseSystemInfo.SystemCode`）。
  - 写方法（如 `BaseUserRoleManager.Add`:37、`BaseModuleManager.UniqueAdd`:37）只 `AddEntity(entity)`，**不自行 stamp `entity.SystemCode`**，依赖调用方赋值；dedup 用 `entity.SystemCode`。
- Mode B 潜在 Bug（写入文档 §8）：
  - B1 🔴 `BaseUserRoleManager.GetDataTable`(:105)/`GetDataTableByPage`(:48) 读不过滤 SystemCode + 缓存键无 systemCode → 共享 BaseUserRole 表下跨子系统泄漏+缓存污染（即原 #4/#5）。
  - B2 🟠 写不集中兜底，漏设 SystemCode 的行落 `"Base"`（数据丢失或被当全局共享）。因用户 Mode A 读按 SystemCode 过滤且“正常”，推断调用方已写时设 SystemCode，故大概率 OK，但缺硬兜底。
  - B3 🟡 `RemoveCache` 用全局 `BaseSystemInfo.SystemCode`，多子系统不精确。
  - B4 🟡 #7 拼接写法。
  - B5 ⚪ 部署前置：BusinessXxx→BaseXxx 数据迁移（保留 SystemCode 列）。
- 正面：Mode B 下原 #6“默认构造落统一表”反成正确，GetXTableName 退化为 no-op，表路由更健壮；已具备读过滤的有 BaseModule/BaseRole/BasePermission/BaseLogonLog/BaseParameter/BaseUserRole.GetList/BaseUserLogon。
- 最小改造清单（待确认）：B1 补过滤+缓存键；B2 在 BaseManager.AddEntity/UpdateEntity 集中兜底 stamp SystemCode；B3 RemoveCache 实例级；B5 数据迁移。未改代码。
- 文档 `SystemCode-Coverage-Analysis.md` 新增 §8「Mode B 切换评估」。未自动 git commit/push。
