# 2026-09-21 工作记录

## Mode B SystemCode 读路径全库排查与 B1 全量修复

### 用户口径（关键决策）
- 不论 `BaseSystemInfo.UseBaseTable` 为 True/False，**都无条件追加** `AND SystemCode = N'<SqlSafe(effectiveSystemCode)>'`（两种模式统一过滤，取消"仅 Mode B 守卫"）。
- effectiveSystemCode 来源：无 systemCode 参数的方法用实例 `UserInfo.SystemCode`→`BaseSystemInfo.SystemCode`→`"Base"`（与 B3 同源）；有 systemCode 参数的方法直接用该参数（空回退 `"Base"`）。
- 延续 §8.6 既定写法：**SqlSafe 转义，不走真参数化**（因 `override GetDataTableByPage` 等走 `GetRecordByPage` 存储过程、不转发命名参数）。

### 本轮改动（B1 共 5 处，全部落地）
1. `BaseUserRole/BaseUserRoleManager.cs:48` override `GetDataTableByPage` — 补 SystemCode 过滤。
2. `BaseUserRole/BaseUserRoleManager.cs:105` `GetDataTable(bool)` — 补过滤 + 缓存键并入 systemCode（`"Dt."+effSC+"."+CurrentTableName+...`）。
3. `BaseRole/BaseRoleManager.Manual.cs:819` `GetUserDataTable` — 子查询 `FROM BaseUserRole WHERE RoleId=@RoleId AND Deleted=0` 加 SystemCode。
4. `BaseUser/BaseUserManager.Manual.Role.cs:452` `GetUserRole` — 子查询加 SystemCode + 缓存键防撞（`"Dt."+(sc??"Base")+"."+GetUserRoleTableName(sc)`）。
5. `BaseUser/BaseUserManager.Manual.Role.cs:802` `GetDataTableByCompanyByRole` — 子查询加 SystemCode。

### 全库排查结论
- 静态扫描 `DotNet.Business/**/*.cs`，标记"查 SystemCode 分区表且 WHERE 无 SystemCode"的读方法 → 候选 5，剔除误报 2（`BaseStaff` 地址表非分区表，仅注释含 `BaseRole` 字面被扫中）。
- 3 个真缺口（#3~#5）已修复；`BaseUserManager.GetUserRoleDataTable`（~:752）已自带 SystemCode 过滤（§8.6 B4）+缓存键含 systemCode，非缺口。
- grep `UseBaseTable` 确认现存无条件 SystemCode 过滤点（BaseModule/BaseRole/BasePermission 等）未受 `UseBaseTable` 守卫，两种模式本就过滤，无需改。

### 验证
- `dotnet build DotNet.Business -f net8.0 -c Release`：0 错误（1721 警告，既有 CA 文化/全球化类）。
- `dotnet build DotNet.Business -f net48 -c Release`：0 错误（73 警告，既有 CS1573）。
- 未自动提交（遵循约定，待用户确认）。

### 文档
- `SystemCode-Coverage-Analysis.md`：§8.2 B1 行改 ✅ 已修（5 处）；§8.3/§8.4 同步；新增 §8.9 修订记录（含全库排查方法、残留 B2/B5、RemoveCache 缓存键对齐残留、两种模式统一过滤的 Mode A 前置条件风险）。

### 待办（未动）
- B2（🟠）：写路径集中 stamp（`BaseManager.AddEntity/UpdateEntity` 兜底 `entity.SystemCode`）。
- B5（⚪）：数据迁移 `BusinessXxx → BaseXxx` 保留 SystemCode 列。
- RemoveCache 缓存键与 `GetDataTable` 新键不精确命中（需 CacheUtil 前缀删除才能闭环）。

## RemoveCache 缓存键对齐（B1 残留闭环）— 2026-09-21

- 用户指令：B5 不做；RemoveCache 缓存键对齐推进；DB 回归等明天部署数据库再做。
- 根因：`GetDataTable` 缓存键 = `"Dt."+effSC+"."+CurrentTableName+"."+companyId+"."+flag`（如 `Dt.Base.BaseUserRole.123.1`）；`RemoveCache` 仅清 `Dt.<effSC>.UserRole`（缺表名段）+ `List.*.UserRole` 等历史键 → 不命中。更隐蔽：`UpdateEntity<T>` 调 `RemoveCache(entity.Id)`（int 重载），基类 `RemoveCache(int id)` 只删实体缓存、不调无参 `RemoveCache()` → 更新路径根本不清除列表/表缓存。
- 改动 `src/DotNet.Business/BaseUserRole/BaseUserRoleManager.Cache.cs`：
  - 无参 `RemoveCache()` 顶部统一算 `effSC`（与 B1/B3 同源，原 `UserInfo==null` 硬编码 "Base" 改为一致的 effSC 推导）；新增 `CacheUtil.RemoveByRegex("^Dt\."+Regex.Escape(effSC)+"\."+Regex.Escape(CurrentTableName)+"\.")` 前缀删除覆盖 `GetDataTable` 全部 companyId/flag 组合（Mode A/B 共用，CurrentTableName 随模式变）；兼容历史键一并清。
  - 新增 `RemoveCache(int id)` override：先调无参 `RemoveCache()`（使 B1 对齐在 Add/Update/SetEnabled/SetDeleted 全写路径生效），再保留实体缓存清除语义。
- 验证：net8.0 / net48 均 **0 错误**（警告均为既有 CA/CS1573，无关）。
- 文档 `SystemCode-Coverage-Analysis.md`：§8.9.5、§8.10.5 的 RemoveCache 残留项标 ✅ 已修；结尾注记补；新增 §8.11 修订记录。
- 范围仅限 `BaseUserRoleManager`；其它管理器（BaseRole/BasePermission/BaseModule）同类错位未扩范围处理（如需另开轮）。B5 按用户决定不做。DB 回归待部署 SQL Server。
- 未自动 git commit/push（遵循约定，待用户确认）。
