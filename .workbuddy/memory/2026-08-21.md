# 2026-08-21 工作日志

## 代码库彻底检查 + 高/中优先级 Bug 修复（1-9 项）

以资深 C# 架构师视角对 DotNet.Util 做静态+编译检查。net8.0 编译 0 个代码错误（仅 DotNet.Test / Web.UI.BasePage 因非 net8 目标报 NETSDK1005，属配置；另有 817 条 TFM 不支持警告）。

修复清单（已落盘，未 commit/push，等用户确认）：
1. ExcelUtil.cs:223 `Replace(".",...)` 误伤路径所有点 + 整数相加丢日期 → 改为 `Path.GetFileNameWithoutExtension`+时间戳+.html
2. DbHelper.OpenAsync 重复调用未释放旧连接 → 补齐释放（与同步 Open 一致）
3. 7 处 Stream.Read 未检查返回值（HttpUtil/FileUtil/IpUtil/QqwryUtil/BaiduOcrUtil/EncodingUtil/VerifyCodeImage）→ 改循环读取
4. DbHelper.Dispose 事务中仍关连接 → `_dbConnection` 关闭加 `!InTransaction` 保护
5. BaseLogonLogManager.Report.cs 日期拼 SQL 注入 → 校验后 `.ToDateTime().ToString()` 重解析为安全字面量
6. SecretUtil.cs:156 注册时间炸弹（2020-08 后恒 false）→ 移除过期逻辑，CheckRegister 默认 true
7. WeChatMiniProgramUtil / BaiduOcrUtil 同步阻塞异步死锁 → `ConfigureAwait(false).GetAwaiter().GetResult()`
8. 全局锁 UserLock 跨缓存/权限/人员共用 → 拆为各 class 独立锁（_locker/_permissionLock/_cacheClearLock/_permissionClearLock）
9. csproj net6/7 引用 Extensions 9.0.9 不支持 → 先加 SuppressTfmSupportBuildWarnings（仅抑制），后按用户要求改为「真正兼容」：DotNet.Util.csproj 与 DotNet.Util.Cache.csproj 引入按 TFM 版本变量（net6/7 的 Extensions 用 8.0.0 以满足 AspNetCore.Hosting 2.3.0 的 >=8.0.0 约束；System.* 用 6.0.0；其余 9.0.9）。net6/net7/net8 构建验证通过。

验证：DotNet.Util / Plus / Db / Business 四个项目 net8.0 构建均 0 错误。Web.UI.BasePage 仅 net4x，无法在本机编译，但改动为锁重命名+私有静态字段，语法有效。

## 低优先级 Bug 修复（10-12 项）
10. Utils.cs:1511 `ClearLastChar`：str 为 null 时 `str.Substring` NRE → 改 `string.IsNullOrEmpty(str)`
11. Utils.cs:1488 `IsRuleTip` timesect 分支：`Split('-')[1]` 越界 → 先判 `splitetime.Length < 2`
12. Utils.cs:1066 `GetPageNumbers`：`url` 为 null 时 `url.IndexOf` NRE → 改 `!string.IsNullOrEmpty(url) && url.IndexOf(...)`；WebUpload.cs:564 `fileExt.ToLower()` 空值 NRE → 加 `!string.IsNullOrEmpty(fileExt)` 守护

验证：DotNet.Util net8.0 重新构建 0 错误。1-12 项全部修复完成（均未 commit/push）。

## 为 NuGet 项目生成 README（2026-08-21 晚）
为全部 13 个 GeneratePackageOnBuild=true 的 NuGet 包项目在其文件夹下新增中英双语 README.md：
DotNet.Util、DotNet.Util.Plus、DotNet.Util.Db、DotNet.Util.Cache、DotNet.Model、DotNet.Business、DotNet.Business.Web、DotNet.Web.UI.BasePage、DotNet.Util.Db.MySql/OleDb/Oracle/PostgreSql/SQLite。
内容：简介、包含内容、dotnet add package 安装、目标框架、依赖（含按 TFM 版本差异）、快速使用、MIT 许可证、仓库链接。
注意：DotNet.Web.UI.BasePage 仅 net4x；DotNet.Util.Db.PostgreSql 不含 netstandard2.0 且 VersionPrefix=1.1（其余 1.2）。

## 第二轮审查 + TryParse 修复（#4 文化依赖 / #8 数值 Parse 无护航）
第二轮审查新发现 8 项（路径穿越/XXE/空 catch/DES 弱加密等），用户仅要求修复涉及 TryParse 的两类：
- #4 DateTime.Parse 文化依赖（10 处）：BaseLogManager.cs:204-208、BasePermissionScopeManager.cs:1399-1407、BaseModuleManager.Manual.cs:570-578、BaseOrganizationManager.Manual.cs:506-524、DateUtil.cs:440（ToDate 改为 TryParse 双兜底，失败返回 Today）、ValidateUtil.cs:287（IsDateTime 加强判断改 TryParse）、ExcelUtil.Export.cs 4 处（IsDateTime+Parse 组合替换为 TryParse）
- #8 数值 Parse 无护航（6 处）：IpUtil.cs:133（byte×4 TryParse 失败返回空数组）、StringUtil.cs:487（byte.TryParse hex）、BaseManager.GetDatabaseSize.cs:57（decimal.TryParse 失败 0）、JsonUtil.cs:121（long.TryParse 失败返回原 match）、WeChatMiniProgramUtil.cs:150（int.TryParse 失败默认 7200 秒）、BaiduOcrUtil.cs:97（失败默认 2592000 秒）
用户输入点（BaseLog/CheckDateScope/ToDate/IsDateTime）采用「当前文化 TryParse || InvariantCulture 兜底」双解析。验证：Util/Plus/Business net8.0 构建 0 错误（Plus 首次并行构建报 CS2012 文件锁冲突，系并行构建竞争非代码问题，单独重跑通过）。全库已无裸 Parse（除注释代码）。

## 第二轮剩余项修复（#1/#2/#3/#5/#6）
- #1 ExcelUtil.SaveExcelFile 路径穿越：file.FileName 改用 Path.GetFileName 消毒 + GetFileNameWithoutExtension 安全插时间戳，无扩展名不再抛异常
- #2 ExcelUtil.EntityListToExcel2003：sheetName 用 Path.GetInvalidFileNameChars 消毒 + 防 ..，CreateSheet 同步用消毒名
- #3 WebUtil.UpLoadFile 目录穿越：新增 private static SanitizePathSegment（Path.GetFileName + 非法字符过滤），categoryId/objectId 消毒后拼目录，fileName 参数分支也 GetFileName；注意 WebUtil.cs 整文件在 #if NET46_OR_GREATER 内（仅 net4x 编译，本机无法验证）
- #5 BaseSystemInfo.CreateOpenId：删除 Thread.Sleep(10000)（前后无等待条件，纯硬编码延迟）
- #6 SecretUtil DES 弱加密：因 DesEncrypt/DesDecrypt 被 UserConfigUtil/ConfigurationUtil 几十处持久化配置（连接串/邮箱密码）使用，直接换算法会破坏旧数据 → 新增 AesEncrypt/AesDecrypt（AES-CBC+随机IV前置+SHA256派生密钥+Base64输出）+ Sha256Bytes，DES 方法保留并加警示注释
验证：DotNet.Util / DotNet.Util.Plus net8.0 构建 0 错误。剩余未修：#7 空 catch（Utils.cs:1810/1948、DisposeBase.cs、EncodingUtil.cs）。

## #7 空 catch 吞异常（完成，第二轮审查全部收尾）
- Utils.cs GetSourceTextByUrl：catch { return ""; } → catch (Exception ex) + LogUtil.WriteLog + return ""
- Utils.cs RestartIisProcess：catch {; } → catch (Exception ex) + LogUtil.WriteLog
- NewLife/DisposeBase.cs 两处（TryDispose 释放成员/对象）：catch { } → catch (Exception ex) + LogUtil.WriteLog（Dispose 不向上抛，保留 using 语义）
- NewLife/EncodingUtil.cs IsMatch（编码探测）：catch { } → catch (Exception) + 注释（探测多候选编码时解码失败属正常路径，写日志会刷屏，故明确捕获 Exception 并注释意图，不写日志）
验证：DotNet.Util net8.0 构建 0 错误，全库已无空 catch。至此第二轮审查 8 项全部处理完毕（#4/#8 TryParse 修复、#1/#2/#3/#5/#6 修复、#7 空 catch）。所有改动未 commit/push。

## 第三轮审查（2026-08-22 凌晨，仅列清单未修复）
聚焦并发/边界/资源/编码，新发现 6 类：
- #1 Encoding.Default 约 20 处（Utils/SecretUtil DES 加解密/QqwryUtil/BaiduOcrUtil/XmlUtil 等）：Framework 上=ANSI/GBK、Core 上=UTF-8，跨运行时迁移 DES 密文不可解、字节统计/协议解析不一致
- #2 Encoding.GetEncoding("gb2312")（FileUtil.cs:661 等）：.NET Core 默认不可用（需注册 CodePagesEncodingProvider），net8 编译过但运行时可能抛
- #3 资源泄漏（异常路径跳过释放）：WebUpload.RemoteSaveAs client.Dispose 在 catch early-return 外、CsvUtil.ToDataTable fs/sr Close 无 finally、Utils.GetSourceTextByUrl response/resStream/sr 全未释放、FileUtil.GetTextFileContent sr 无 finally、HttpUtil.cs:61 sr 未入 finally
- #4 new Thread 每次调用（BaseLogonLog 311/534、BaseUserLogon 987、MailUtil 292）：高并发线程风暴，建议 Task.Run
- #5 WebUpload.cs:329 catch { return string.Empty; } 裸吞异常（#7 漏网）
- #6 static ConcurrentDictionary（ReflectionUtil.cs:182/187）线程安全 OK 不列
静态集合多为局部变量；无硬编码密码；取模/范围判断正确。等待用户确认修复范围。
