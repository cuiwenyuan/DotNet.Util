# 2026-09-20 工作日志

## 修复 Mode B 潜在 Bug B3（BaseUserRoleManager.RemoveCache 实例级 SystemCode）

- 用户要求“先修复 B3”。
- B3：原 `BaseUserRoleManager.Cache.cs` `RemoveCache`（:44-50）三处缓存键用全局 `BaseSystemInfo.SystemCode`，多子系统 Mode B 下只清全局子系统缓存、漏清其它子系统 UserRole 缓存。
- 改动：改用实例级 `effectiveSystemCode = UserInfo.SystemCode`（若空则回退 `BaseSystemInfo.SystemCode`），用于 `cacheKeyListSystemCode` / `cacheKeySystemCode` / `cacheKeySystemCodeUserId`。`BaseUserRoleManager` 无独立 systemCode 字段，最可靠实例级来源即 `UserInfo.SystemCode`（登录时按子系统赋值，见 `WebUtil.LogOn.cs:642/731`）。单子系统部署行为不变（无回归）。
- 范围控制：仅修 B3；未动 B1（`GetDataTable`/`GetDataTableByPage` 过滤+缓存键）、B2（写集中兜底）、B4（拼接参数化）、B5（数据迁移）。已知残留：`GetDataTable` 缓存键不含 systemCode，`RemoveCache` 的 `cacheKeySystemCode` 仍不命中它（B1 范畴）。
- 构建：net8.0 / net48 均 **0 错误**（29 / 40 警告，均为既有 NU1603 / CS1573，与本次无关）。
  - ⚠️ 沙箱偶发：某次增量重建因沙箱拒绝删除/写入 `bin/obj`（`删·拒绝`/`读/写·拒绝`）报“1 个错误 / 1246 警告”，重跑即 0 错误——系环境文件锁/沙箱拦截，非代码错。
- 文档 `SystemCode-Coverage-Analysis.md`：§8.2 B3 行改 `✅ 已修`，新增 §8.5 修订记录（2026-09-20 实施 B3）。
- 未自动 git commit/push（遵循约定，待用户确认）。下一步可继续 B1（GetDataTable/GetDataTableByPage 补 SystemCode 过滤+缓存键，需实例级 _systemCode 或改基类虚方法签名）。

## 修复 Mode B 潜在 Bug B4（读路径 SystemCode 拼接注入式 → SqlSafe 转义）

- 用户要求“再修下 B4”。B4：读路径把 `SystemCode` 字符串拼接进 SQL（`'"+ systemCode +"'`，部分 `N'`），原 #7 注入式写法。
- 覆盖点（共 25 处 + `BaseParameterManager:97` 此前已 SqlSafe 无需改）：
  - `BaseModuleManager.Manual.cs` `GetDataTableByPage` 14 处（:253/268/280/285/299/311/316/330/342/356/359/360/426/472）；
  - `BaseRoleManager.Manual.cs` `GetDataTableByPage` 5 处（:282/292/303/318/330）；
  - `BaseUserManager.Manual.Role.cs` `GetListByRole`(:596)/`GetDataTableByRole`(:643)/`GetUserRoleDataTable`(:752) + 共享 `if(systemCode.IsNullOrEmpty()){systemCode="Base";} var userRoleTableName=…` 模式的 `ClearUser`/`ClearRole` 等（SqlSafe 对简单值幂等，无害）；
  - `BaseLogonLogManager.Manual.cs` 两个 `GetDataTableByPage`（:64/:171）。
- 改动：每处方法入口对 `systemCode` 调 `dbHelper.SqlSafe(systemCode)`（`BaseUserManager` 用 `DbHelper.SqlSafe`，与该文件既有 `DbHelper.GetParameter` 风格一致）。拼接/SQL 结构不变，注入字符转义为 `''`。
- 关键约束（为何不用真正 ADO.NET 参数化）：`BaseManager.GetDataTableByPage`（:49→:103）SQL Server **表模式分支**调用不含 `dbParameters` 的 `GetDataTableByPage` 重载，会**丢弃参数**；`BaseModule`/`BaseRole` 仅 `userId`/`roleId` 入参时构建 `sbView`（走可转发参数的 SELECT 分支），多数调用走表模式 → 真参数化会抛“必须声明标量变量 @SystemCode”。对比 `BasePermissionManager` 用 `DbHelper.ExecuteReader/Fill(commandText, dbParameters)` 直调才安全（那需手写分页 SQL，较大重构）。故采用与 `BaseParameterManager:96` 一致的 SqlSafe 转义，零回归风险。
- 构建：net8.0 / net48 均 **0 错误**（29 / 71 警告，既有 NU1603/CAxxxx）。沙箱仍偶发 `CS2012`/`CS0016`（obj/bin 写被拒），重跑 `-f net8.0`/`-f net48` 即 0 错误——环境文件锁，非代码错。
- 文档：§8.2 B4 行改 `✅ 已修`，新增 §8.6 修订记录（2026-09-20 实施 B4）；关闭注记更新为“B3、B4 已实现，其余 B1/B2/B5 待确认”。
- 未自动 git commit/push（遵循约定，待用户确认）。后续可选：若用户要真正参数化，需把这几处 `GetDataTableByPage` 重构为手写分页 SQL + `DbHelper.Fill(commandText, dbParameters)`（类 `BasePermissionManager.GetPermissionIdsByUser`）。

## 指令⑤：B4 升级为真正 ADO.NET 参数化（推翻 §8.6 的 SqlSafe 方案）

- 用户要求："真正的 ADO.NET 参数化（而非 SqlSafe）"。
- 新增 opt-in 重载 `BaseManager.GetDataTableByPage(out recordCount, pageNo, pageSize, sortExpression, sortDirection, tableName, condition, selectField, IDbDataParameter[] dbParameters)`（`BaseManager.GetDataTableByPage.cs:124`）：SELECT 分支转发 `dbParameters` 到 `DbHelper.ExecuteScalar`/`GetDataTableByPage`(:46)；表模式分支转发到 `DbHelper.GetDataTableByPage`(:360 内联、真正转发参数，等价于 `GetRecordByPage` sp 语义)。**不翻转**全局 `:103` sp 路径（影响 ~50 调用方），仅 5 个 SystemCode 管理器走新重载。
- 5 个管理器改造（SystemCode 拼接 → `@SystemCode` 占位符 + `dbParameters.Add(MakeParameter(FieldSystemCode, systemCode))`；末尾切 8 参重载）：
  - `BaseModuleManager.Manual.cs` GetDataTableByPage：14 处（含 3 处双空格变体）；
  - `BaseRoleManager.Manual.cs`：5 处；
  - `BaseUserManager.Manual.Role.cs` GetListByRole/GetDataTableByRole/GetUserRoleDataTable：3 方法 3 处；
  - `BaseLogonLogManager.Manual.cs` 两 GetDataTableByPage：2 处（补 `using System.Collections.Generic;`）；
  - `BaseParameterManager.Manual.cs`：1 处（位于 `if(!systemCode.IsNullOrEmpty())` 内，方法级 dbParameters）。
- 残留：`BaseModule`/`BaseRole`/`BaseLogonLog` 保留入口 `SqlSafe(systemCode)`（仅影响已冗余参数值、幂等、对齐 §8.6）；`BaseUser`/`BaseParameter` 直接去掉 `SqlSafe`。
- 发现既有 bug（超范围未修）：`BaseUserManager.Manual.Role.cs:499` `GetRoleIds` 用 `DbHelper.GetParameter(systemCode)`（值当字段名→`@<systemCode值>`）但 `dbParameters` 未加对应参数 → 运行时"必须声明标量变量"风险。待单独修。
- 构建：net8.0 / net48 均 **0 错误**（仅既有 CAxxxx/NU1603 警告）。
- 文档 `SystemCode-Coverage-Analysis.md`：§8.2 B4 行改 `✅ 已升级真参数化`，新增 §8.7 修订记录。
- 未自动 git commit/push（遵循约定，待用户确认）。

## 用户回退参数化 + 顺手修 GetRoleIds 既有 bug（2026-09-20）

- 用户复核后决定放弃"真正 ADO.NET 参数化"风格（其余大量字段未做同类处理），已整体回退指令⑤的代码改动（5 个管理器回到 §8.6 的 SqlSafe 写法；新增的 `BaseManager.GetDataTableByPage` 8 参重载一并移除）。`SystemCode-Coverage-Analysis.md` §8.7 标"已整体回退"、§8.2 B4 行改回"🟡 已实现 SqlSafe 转义"。
- 单独修复发现的既有 bug（不在指令⑤范围）：`BaseUserManager.Manual.Role.cs` `GetRoleIds` 用 `DbHelper.GetParameter(systemCode)`（值当字段名→`@<systemCode值>`）且 `dbParameters` 未加 systemCode 参数 → 运行时"必须声明标量变量"。改为 `DbHelper.GetParameter(BaseUserRoleEntity.FieldSystemCode)` + 在 `dbParameters` 增 `MakeParameter(FieldSystemCode, systemCode)`。文档新增 §8.8。
- 构建：net8.0 / net48 均 0 错误。
- 未自动 git commit/push（遵循约定，待用户确认）。
