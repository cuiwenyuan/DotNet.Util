# 2026-08-22 工作日志

## 第三轮审查修复（#1-#5，用户确认全修，#1 按建议方案）

### #1 Encoding.Default 约 20 处 → 显式编码
- 新增 `Utils.GbkEncoding` 共享 helper（`Utils.cs`，放在 NET46_OR_GREATER 条件块**外**——曾误插入块内导致 net8.0 编译 CS0117，已修正）：条件编译 Framework 原生 gb2312 / Core 注册 CodePagesEncodingProvider，失败兜底 UTF-8
- DES 加解密（SecretUtil.cs）：加密改 `Encoding.UTF8.GetBytes`；解密先 UTF-8 严格解码（new UTF8Encoding(false,true)），DecoderFallbackException 时回退 `Utils.GbkEncoding`（兼容 Framework 时代 GBK 旧密文）
- 拼音（StringUtil.PinyinHelper.cs:230）：必须 GBK（区位码表），改 `Utils.GbkEncoding`
- 纯真 IP 库（QqwryUtil.cs:124）：地区名 GBK → `Utils.GbkEncoding`
- 协议/HTTP 类：BaiduOcrUtil（2 处）、WeChatMiniProgramUtil:225、XmlUtil:295、MachineInfo:338 → 显式 UTF-8
- 通用工具：Utils.cs GetStringLength/GetUnicodeSubString/按字节截取/785、StringUtil.cs:442 → 显式 UTF-8，宽度常量 2→3（对齐 UTF-8 3 字节），注释同步更新

### #2 Encoding.GetEncoding("gb2312") .NET Core 不可用
- csproj（DotNet.Util）加 `<PackageReference Include="System.Text.Encoding.CodePages" Version="$(SystemVersion)" />`
- FileUtil.GetTextFileContent 重构：try/catch + 条件编译获取 gb2312（Framework 原生 / Core RegisterProvider），失败兜底 UTF-8；sr 改 using 释放

### #3 资源泄漏（异常路径跳过释放）5 处
- WebUpload.RemoteSaveAs：`using var client = new WebClient()`（原 Dispose 在 catch early-return 后被跳过）
- CsvUtil.ToDataTable：`using var fs/sr`，删除方法尾无 finally 保护的 Close
- Utils.GetSourceTextByUrl：`using (response) using (sr)` 全释放（原 response/resStream/sr 均未释放）
- FileUtil.GetTextFileContent：using var sr（与 #2 合并处理）
- HttpUtil.cs：3 处相同模式 `using (var sr = ...)` 统一替换（replace_all）

### #4 new Thread → Task.Run（4 处高频滥用）
- BaseLogonLogManager.Manual.cs:311/534（登录日志）、BaseUserLogonManager.Manual.cs:987（访问时间）、MailUtil.cs:292（发邮件）→ `System.Threading.Tasks.Task.Run(() => 方法(tuple))`（全限定名，文件无 Tasks using）
- 保留 BaseSystemInfo.GetNewOpenId 的 new Thread（低频显式触发，合理）

### #5 WebUpload.RemoteSaveAs 裸 catch → `catch (Exception ex)` + LogUtil.WriteLog

## 验证与坑
- 三项目 net8.0 构建全部 0 错误：DotNet.Util、DotNet.Util.Plus、DotNet.Business
- **坑1**：Utils.GbkEncoding 曾误插入 `#if NET46_OR_GREATER` 块内（Utils.cs 29-2383 行大块），net8.0 下整个块排除 → CS0117。移到块外（2383 #endif 之后、GetIp 前）解决
- **坑2**：Business 项目构建反复报 `obj\project.nuget.cache / *.cache / AssemblyInfo.cs Access denied`——Business 的 obj 内旧文件被某进程锁定（沙箱/索引），Util/Plus 不受影响。`dotnet build-server shutdown` 无效；删除 obj 目录（rm -rf 需 dangerouslyDisableSandbox，沙箱内 rm 静默失效）后全新构建成功。属环境问题非代码问题
- 编码修复后全库已无 `Encoding.Default` 代码引用（仅注释提及）

## 第四轮 Code Review（仅列 Bug，未修复，待用户确认）
扫描 402 个 .cs，重点查前几轮未覆盖/修复遗漏点。结论：空 catch、async void、.GetAwaiter 误用、Thread.Sleep（BaseSequence 为有意退避）、Encoding.Default、SQL 注入点（BaseLogonLog/WebUtil）均已在 1-3 轮清除。本轮新发现：
- **中·资源泄漏（异常路径 StreamReader/Writer 未用 using，第3轮 #3 只覆盖部分）**：
  - CsvUtil.cs:331 `ExportCsv(DataTable)`、:350 `ExportCsv(DataSet)` — StreamWriter 仅正常路径 Close，无 using/finally
  - HttpUtil.cs:335 `Delete`、:392 `Get` — 响应 sr 为局部变量，finally 只 Dispose response，sr 泄漏
  - Utils.cs:2273 `HttpPost`、:2315 `HttpGet` — 同 HttpUtil，sr 泄漏
- **中·安全（口令哈希弱）**：`SecretUtil.Md5`（SecretUtil.cs:175-220，无盐 MD5）用于 `BaseUserManager.Manual.SetPassword.cs:42` 存储口令；部分分支 MD5×3。需迁移到加盐哈希（SHA256+salt/PBKDF2/Argon2）+ 版本化兼容（破坏性变更，需规划）
- **低**：Utils.cs:1205 循环 FileStream 仅正常路径 Close；VerifyCodeImage.cs:380/412 MemoryStream（:412 `new Bitmap(ms)` 后 `ms.Close()` 返回依赖已关闭流的 Bitmap，GDI+ 隐患）；DrawingUtil.cs:18 `_cryptoRandom` 名不副实（仅噪点坐标）；Utils.cs:296 通用 Md5（MD5 已破）；Utils.cs:635 GetDate 用 Convert.ToDateTime+try/catch 静默吞（建议 TryParse）；DateUtil.cs:514 可改 `new DateTime(y,m,1)`；WatermarkUtil/ThumbnailUtil `Image.FromStream(new MemoryStream())` 流未释放（Image.FromStream 要求流保持打开，维持现状）
- 未自动修复，等用户确认范围

## 第四轮修复（用户确认"全部修复"）
### A 组资源泄漏 6 处（全部改 using）
- CsvUtil.cs ExportCsv(DataTable)/ExportCsv(DataSet)：StreamWriter 包 using（:350 原连 TryDispose 都没有）
- HttpUtil.cs Delete(:335)/Get(:392)：响应 StreamReader 包 using（原 finally 只 Dispose response）
- Utils.cs HttpPost/HttpGet：sw/sr 均包 using，删除外层 `StreamWriter sw` 声明与 finally 中 `sw=null`（原 finally 不释放 sr）
### 低危
- Utils.cs:1205 循环 FileStream → using
- VerifyCodeImage.cs ToBase64：MemoryStream 包 using；ToImage：`new Bitmap(ms)` 改为先 `new Bitmap(temp)` 复制独立位图再释放流（修 GDI+ 依赖已关闭流隐患）
- DrawingUtil.cs：`_cryptoRandom` → `_noiseRandom`（仅噪点坐标用，验证码值仍走 RandomUtil CSPRNG）
- Utils.cs Md5：XML 文档注明仅限非安全场景
- Utils.cs GetDate：Convert.ToDateTime+try/catch → DateTime.TryParse
- DateUtil.cs WeekOfMonth：`Convert.ToDateTime(y+"-"+m+"-1")` → `new DateTime(y,m,1)`
- WatermarkUtil/ThumbnailUtil `Image.FromStream(new MemoryStream())`：维持现状（Image.FromStream 要求流保持打开，Dispose 反而错）
### B 口令哈希：**纠正误判，未改代码**
- 实查：`EncryptUserPassword` 有盐——`SetPassword` 用 `RandomUtil.GetString(20)`（RandomNumberGenerator/CSPRNG）生成盐存入 `FieldSalt`，哈希为"盐混合 + 多层 MD5"；登录校验（BaseUserManager.Manual.Logon.cs:1444/1745）、改密（ChangePassword.cs:67）、新增（Add.cs:125）均传 `entity.Salt` 参与比对。**并非"无盐 MD5"**，撤回第四轮 B 项
- 残留关注：MD5 无 key stretching（CA5351 警告存在），加固需版本化 KDF 迁移（破坏性），留作后续规划，不动现有逻辑
### 验证
- DotNet.Util / DotNet.Util.Plus / DotNet.Business net8.0 构建全部 0 错误（警告为既有 CA 分析项，未新增）
- 未 commit/push，等用户确认

## 第五轮 Code Review（仅列 Bug，未修复，待确认）
换新维度扫描，重点验证项均已干净：XXE（XmlUtil/UserConfigUtil 均 DtdProcessing.Prohibit+XmlResolver=null）、无 BinaryFormatter/SoapFormatter、无 Process.Start、无 DataTable.Compute、无 Timer/ThreadPool.QueueUserWorkItem、Task.Run fire-and-forget 3 处方法体均有 try/catch、BaseManager.DbHelper 双检锁 static _locker 仅懒初始化短暂持有非瓶颈。新发现：
- **中**：BaiduOcrUtil.cs:78 GetAccessToken 中 `new HttpClient()` 与 `response` 正常路径即未释放（应 using）；WebUtil.BuildUrl（WebUtil.Interface.cs:30-42）`paramText` 直接插值进 Regex 未 Escape（参数名含正则元字符时替换失效→URL 参数重复/畸形），`paramValue` 未 URL 编码直接拼 URL
- **低**：HttpRequestUtil.cs WcGet/WcPost/WcPostJson（:26/42/60）WebClient 未释放（与已修的 WebUpload.cs:289 不一致）；WebUtil.LogOn.cs:562 WebClient 未释放（net4x）；全库 Regex 均无超时（ReDoS 暴露，含 ValidateUtil 用户可传 pattern 场景）；UserConfigUtil.cs:473/748/752 TryParse 返回值未检查（CA1806，失败静默取 0）；XmlConfigUtil.cs:97 仅 XmlResolver=null 未显式 DtdProcessing.Prohibit（本地受信文件低风险）；RandomUtil 循环字符串拼接（风格项）
- 待用户确认修复范围

## 第五轮修复（用户确认"所有找到的都逐一修复"）
### 中危
- BaiduOcrUtil.cs GetAccessToken：`new HttpClient()` + `PostAsync` 响应包 `using`（正常路径泄漏修复）
- WebUtil.BuildUrl（WebUtil.Interface.cs）：`Regex.Escape(paramText)` 防正则元字符注入、`Uri.EscapeDataString(paramValue)` 防 URL 结构破坏、两个 Regex 加 1s 超时
### 低危
- HttpRequestUtil.cs WcGet/WcPost/WcPostJson：WebClient 包 using（与 WebUpload.cs:289 对齐）
- WebUtil.LogOn.cs:562：WebClient 包 using（net4x，net8.0 下亦编译通过）
- Regex 超时（ReDoS 加固，统一 `TimeSpan.FromSeconds(1)`）：ValidateUtil.cs（5 处 new Regex(regexString) 用 replace_all + IPv6×2 + CheckEmail + IsNumeric×3 + 身份证×2 + 车牌 + QQ）、Validation.cs（IsEmail/IsMobile/IsPassword/IsLoginName/IsQq，补 `using System;`——曾 CS0103）、WordUtil.TemplateExport.cs（×2）、RedisUtil.cs（Redis key pattern 匹配）
- UserConfigUtil.cs：Port/OnlineLimit/SlowQueryMilliseconds 三处 TryParse 失败时 `LogUtil.WriteLog` 告警（原 CA1806 静默取 0）
- XmlConfigUtil.cs Load：改 `XmlReader.Create(settings{DtdProcessing.Prohibit, XmlResolver=null})` + `_doc.Load(reader)`（原仅 XmlResolver=null）
- RandomUtil.GetString/GetNumber：循环拼接 → `System.Text.StringBuilder(length)`（O(n²)→O(n)）
### 验证
- DotNet.Util / DotNet.Util.Plus / DotNet.Util.Cache / DotNet.Business.Web 四项目 net8.0 构建全部 0 错误
- 未 commit/push，等用户确认

## NuGet 版本全面升级（逐包查 nuspec 兼容矩阵后更新 10 个 csproj）
### 升级明细（"最新版"= 各 TFM 下支持的最新版，旧 TFM 保持兼容钉版）
- **DotNet.Util**：NewLife.Core 11.7.2025.1001→**11.18.2026.801**；ExtensionsVersion 9.0.9→**10.0.11**、SystemVersion 9.0.9→**10.0.11**（net8/9/10/ns2.0/2.1）；Microsoft.AspNetCore 系列 2.3.0→**2.3.12**（6 包）；net6/7 SystemVersion 6.0.0→**8.0.30**+新增 SystemVersionLegacy=8.0.0（Management/CodePages 只有 8.0.0）——**清除 NU1903（Windows.Compatibility 6.0.0 高危）与 NU1902（传递 SqlClient 4.8.3 中危）**
- **Cache**：ExtensionsVersion→10.0.11；NewLife.Redis→**6.6.2026.801**
- **Plus**：NPOI 2.7.5→**2.8.0**（ns2.0+/net6+；net472/net48 从 2.5.6 也升 2.8.0，net46/47 保持 2.5.6）；System.DirectoryServices→**10.0.11**；Net.Http.Headers net10→**10.0.11**（仅支持 net10）、net9 保持 9.0.9、ns2.0/2.1/net6/7 2.3.4→**2.3.12**（修 NU1605：AspNetCore.Http 2.3.12 要求 >=2.3.11）；SharpZipLib 1.4.2 已最新（1.4.2 无 net46 资产，net46/47 保持 1.3.3）
- **Db 系**：System.Data.SqlClient 4.9.0→**4.9.1**；System.ValueTuple 4.6.1→**4.6.2**（net46/461 保持 4.5.0，4.6.2 已无 net46 资产）；MySql.Data 9.4.0→**26.7.0**（net46/461 保持 8.0.32.1，26.x 需 net462+）；Npgsql 9.0.4→**10.0.3**（仅 net8/9/10，net6/7 保持 9.0.4、net47/48/ns2.1 保持 8.0.3、net46 保持 4.0.17）；Oracle ManagedDataAccess(+Core) 23.9.1→**23.26.300**（net46 保持 19.22.0、net462/47 保持 21.20.0、ns2.0 保持 Core 2.19.290——23.x 无这些资产）
- **Business.Web/BasePage**：JWT 11.0.0→**11.1.0**
- 已最新无需动：Newtonsoft.Json 13.0.4、System.Data.SQLite.Core/Stub 1.0.119、SharpZipLib 1.4.2、Portable.BouncyCastle 1.9.0、System.ComponentModel.Annotations 5.0.0、Microsoft.CSharp 4.7.0、System.Runtime.Loader 4.3.0、System.Memory 4.6.3
### 验证（41 个构建全 0 错误）
- Util：net46/47/48/6.0/7.0/8.0/9.0/10.0/ns2.0（ns2.1 未单独跑）；Plus：net46/8.0/9.0/10.0/ns2.0；Cache：net46/8.0/9.0/10.0/ns2.0；Db+MySql+Oracle+SQLite+OleDb：net46+net8.0；PostgreSql：net46/ns2.1/net8.0；Model net8.0；Business net8.0；Business.Web net46/8.0/10.0；BasePage net46；Test net6.0
- 过程坑：NU1605（Net.Http.Headers 2.3.4→2.3.12）、NU1902/1903（net6/7 SystemVersion 6.0.0→8.0.30）均已修复；全库构建后无 NU 错误/漏洞警告
- 未 commit/push，等用户确认
