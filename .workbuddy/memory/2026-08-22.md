# 2026-08-22 工作日志

## 第三轮审查修复（#1-#5，用户确认全修，#1 按建议方案）

### #1 Encoding.Default 约 20 处 → 显式编码
- 新增 `Utils.GbkEncoding` 共享 helper（`Utils.cs`，放在 NET46_OR_GREATER 条件块**外**——曾误插入块内导致 net8.0 编译 CS0117，已修正）：条件编译 Framework 原生 gb2312 / Core 注册 CodePagesEncodingProvider，失败兜底 UTF-8
- DES 加解密（SecretUtil.cs）：加密改 `Encoding.UTF8.GetBytes`；解密先 UTF-8 严格解码（new UTF8Encoding(false,true)），DecoderFallbackException 时回退 `Utils.GbkEncoding`（兼容 Framework 时代 GBK 旧密文）
- 拼音（StringUtil.PinyinHelper.cs:230）：必须 GBK（区位码表），改 `Utils.GbkEncoding`
- 纯真 IP 库（QqwryUtil.cs:124）：地区名 GBK → `Utils.GbkEncoding`
- 协议/HTTP 类：BaiduOcrUtil（2 处）、WeChatMiniProgramUtil:225、XmlUtil:295、MachineInfo:338 → 显式 UTF-8
- 通用工具：Utils.cs GetStringLength/GetUnicodeSubString/按字节截取/785、StringUtil.cs:442 → 显式 UTF-8，宽度常量 2→3（对齐 UTF-8 3 字节），注释同步更新

### #2 Encoding.GetEncoding("gb2312") .NET Core 不可用
- csproj（DotNet.Util）加 `<PackageReference Include="System.Text.Encoding.CodePages" Version="$(SystemVersion)" />`
- FileUtil.GetTextFileContent 重构：try/catch + 条件编译获取 gb2312（Framework 原生 / Core RegisterProvider），失败兜底 UTF-8；sr 改 using 释放

### #3 资源泄漏（异常路径跳过释放）5 处
- WebUpload.RemoteSaveAs：`using var client = new WebClient()`（原 Dispose 在 catch early-return 后被跳过）
- CsvUtil.ToDataTable：`using var fs/sr`，删除方法尾无 finally 保护的 Close
- Utils.GetSourceTextByUrl：`using (response) using (sr)` 全释放（原 response/resStream/sr 均未释放）
- FileUtil.GetTextFileContent：using var sr（与 #2 合并处理）
- HttpUtil.cs：3 处相同模式 `using (var sr = ...)` 统一替换（replace_all）

### #4 new Thread → Task.Run（4 处高频滥用）
- BaseLogonLogManager.Manual.cs:311/534（登录日志）、BaseUserLogonManager.Manual.cs:987（访问时间）、MailUtil.cs:292（发邮件）→ `System.Threading.Tasks.Task.Run(() => 方法(tuple))`（全限定名，文件无 Tasks using）
- 保留 BaseSystemInfo.GetNewOpenId 的 new Thread（低频显式触发，合理）

### #5 WebUpload.RemoteSaveAs 裸 catch → `catch (Exception ex)` + LogUtil.WriteLog

## 验证与坑
- 三项目 net8.0 构建全部 0 错误：DotNet.Util、DotNet.Util.Plus、DotNet.Business
- **坑1**：Utils.GbkEncoding 曾误插入 `#if NET46_OR_GREATER` 块内（Utils.cs 29-2383 行大块），net8.0 下整个块排除 → CS0117。移到块外（2383 #endif 之后、GetIp 前）解决
- **坑2**：Business 项目构建反复报 `obj\project.nuget.cache / *.cache / AssemblyInfo.cs Access denied`——Business 的 obj 内旧文件被某进程锁定（沙箱/索引），Util/Plus 不受影响。`dotnet build-server shutdown` 无效；删除 obj 目录（rm -rf 需 dangerouslyDisableSandbox，沙箱内 rm 静默失效）后全新构建成功。属环境问题非代码问题
- 编码修复后全库已无 `Encoding.Default` 代码引用（仅注释提及）
