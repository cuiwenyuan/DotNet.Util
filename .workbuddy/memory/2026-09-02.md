# 2026-09-02

## Round9 Code Review（严格 pass，仅审查未改动代码）

以「苛刻技术大神」视角精读前几轮未覆盖的高危文件，产出 `CodeReview-Round9-hidden-bugs.md`。本轮**只审查、未改代码**。

🔴 P0（3）：
- R9-1 `SecretUtil.Md5/Sha1` 用于口令存储：**无盐 + 快速哈希(MD5/SHA1)**，仓库 `BaseUserManager.SetPassword` 实际依赖 → 应改 PBKDF2/bcrypt/Argon2 加盐。
- R9-2 `JsonUtil.Manual.JsonToDataTable` 用字符串手术（`Replace(",\"","*\"")`/`Replace("\":","\"#")` + `IndexOf("]")`）解析 JSON → 含 `:`/`,` 的字符串值、嵌套数组静默损坏/异常。应改 Newtonsoft `JArray`→`DataTable`。
- R9-3 `ValidateUtil.UnsafeCharacter/CheckEmail/IsQq` 无 null 保护 → 传 null 抛 NRE/ArgumentNullException。

🟠 P1（7）：R9-4 `IsNumeric(object)` 用 `Convert.ToString`(当前区域)喂 `InvariantInfo` TryParse → 非 invariant 区域装箱数值误判（de-DE 下 `IsNumeric(1234.56d)`=false）；`IsDouble` 同病且不支持负号；R9-5 `JsonSplit.Split` 用 `OrdinalIgnoreCase` 存键 → 大小写变体键静默丢数据；R9-6 `GetJsonStr` 拼 JSON 不转义；R9-7 `GetJosnValue` 回退截取对嵌套/转义/`OrdinalIgnoreCase` 错误；R9-8 `ToXml` 属性值不 XML 转义；R9-9 `IsSqlSafe` 黑名单可绕过 + `SqlSafe` 仅转义单引号；R9-10 `AesEncrypt/Decrypt` AES-CBC 无完整性(AEAD/HMAC)。

🟡 P2（5）：R9-11 `JsonSplit.Split` `catch{}` 吞异常；R9-12 `IsJson` 仅查首尾；R9-13 `SignData/VerifyData` 用 ASCIIEncoding 处理非 ASCII 数据；R9-14 `Encode/DecodeBase64` `catch{}` 吞异常；R9-15 `IsEmail` TLD 限 2-4 字母拒长 TLD。

已通读无实质缺陷：`ExpressionEvaluator.cs`（递归下降+InvariantCulture+除零保护，质量好）、`SqlUtil.cs`（仅参数化日志，无注入）、`RandomUtil.cs`（RandomNumberGenerator+拒绝采样，密码学正确）、`ObjectUtil.cs`（本 partial 仅 ToList）。

下一步：待用户确认修复范围与优先级后开工（建议先 R9-3 null 守卫低成本修，再 R9-1 口令哈希、R9-2 JsonToDataTable）。

## R9-3 已修复（用户"先修复R9-3吧"）

`ValidateUtil.cs` 三处公共方法加 null 守卫，零行为变更：
- `UnsafeCharacter(null)` → `false`（原 `expression.IndexOf` 抛 NRE）
- `CheckEmail(null)` → `false`（原 `email.Trim()` 抛 NRE）
- `IsQq(null)` → `false`（原 `Regex.IsMatch(null)` 抛 ArgumentNullException）
新增 3 个 null 守卫回归测试于 `ValidateUtilTests.cs`；`ValidateUtilTests` 运行 **36/36 全绿**（net8.0，绕开 IDE obj 锁命令）。
`CodeReview-Round9-hidden-bugs.md` 新增「六、修复进度」表，R9-3 标 ✅ 已修+测，其余项 ⏸ 待办。
按用户规矩：**未提交**。改动文件：`ValidateUtil.cs`、`ValidateUtilTests.cs`、`CodeReview-Round9-hidden-bugs.md`。

## R9-5 与 R9-6 已修复（用户"请修复R9-5和R9-6"）

**R9-5**（JSON 键大小写不敏感丢数据）：
- `JsonUtil.Split.cs` `Split` 两处字典 `StringComparer.OrdinalIgnoreCase` → `StringComparer.Ordinal`（JSON 键大小写敏感，杜绝 `Id`/`id` 静默丢数据）；`JsonUtil.Manual.cs` `GetJosnValue` 回退 `IndexOf` 同步改 `Ordinal`。
- 行为变更：JSON 键现严格大小写敏感。修正既有测试 `JsonSplitTests.Split_KeyIsCaseInsensitive`（把旧错误当预期）→ `Split_KeyIsCaseSensitive`（断言 `result[0]["Name"]=="Troy"` 且不含 `name`/`NAME`）。新增 `Split_PreservesCaseVariantKeys`/`GetJosnValue_CaseSensitiveLookup`。

**R9-6**（`GetJsonStr` 拼 JSON 不转义）：
- `JsonUtil.Manual.cs` `GetJsonStr` 改用 `JsonConvert.SerializeObject(jsonDict ?? new Dictionary<string,string>())`（Newtonsoft 自动正确转义 `"` `\` 控制字符，兼容 null）。移除手写 StringBuilder 拼接。
- 新增 `GetJsonStr_EscapesSpecialChars`（值含 `"`/`\`/换行，反序列化回来一致）/`GetJsonStr_NullDict_ReturnsEmptyObject`。

验证：JsonUtilManualTests **20/20** 全绿；全量非集成回归 **1143 通过 / 0 失败**（初跑 2 失败中 1 个是 `Split_KeyIsCaseInsensitive` 旧预期已修正，另 1 个为已知 HttpUtil 网络偶发未复现）。
改动文件：`JsonUtil.Split.cs`、`JsonUtil.Manual.cs`、`JsonSplitTests.cs`、`JsonUtilManualTests.cs`、`CodeReview-Round9-hidden-bugs.md`（进度表 R9-5/R9-6 标 ✅）。
按用户规矩：**未提交**。
